# --- Core web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# --- Database ---
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0

# --- Validation / settings ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
email-validator>=2.0,<3.0

# --- HTTP client (Fakturownia, read-only) ---
httpx>=0.27,<1.0

# --- Security ---
PyJWT>=2.8,<3.0
bcrypt>=4.1,<5.0
# Fernet encryption of integration secrets stored in the database
cryptography>=42.0,<46.0

# --- Scheduling (incremental sync) ---
APScheduler>=3.10,<4.0

# --- Exports ---
openpyxl>=3.1,<4.0
fpdf2>=2.7,<3.0
